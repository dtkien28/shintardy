{
  "name": "shintardy-monorepo",
  "version": "1.0.0",
  "description": "Shintardy Monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
  "start": "node src/server.js",
  "build": "npx prisma generate && npx prisma db push"
}
}
